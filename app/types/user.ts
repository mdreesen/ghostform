export interface Lead {
  name: string;
  email: string;
  address: string;
  goal: string;
  sqft: string;
  budget: string;
  message: string;
}

export interface Company {
  category: string;
  company_name: string;
  company_email: string;
}